using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPoint : MonoBehaviour
{
    // determines if the spawn point has a pick up
    [HideInInspector]
    public bool m_bHasPickup = false;
}